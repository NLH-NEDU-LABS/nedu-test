/**
 * @deprecated Re-export stub — will be removed in Phase 8 cleanup.
 * Import from '@/types/assessment' still works,
 * but prefer '@/features/maxdiff/types' for new code.
 */
export type {
  Problem,
  MaxDiffSet,
  CourseMapping,
  Persona,
  PersonaRoute,
  SetAnswer,
  ScoredItem,
  CourseRecommendation,
  AssessmentResult,
} from '@/features/maxdiff/types';
