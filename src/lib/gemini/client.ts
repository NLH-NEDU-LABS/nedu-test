// src/lib/gemini/client.ts
// Re-exports everything from the original gemini.ts —
// this is the canonical location after Phase 1.
export { getGeminiModel, geminiGenerate, geminiGenerateJSON } from '@/lib/gemini';
