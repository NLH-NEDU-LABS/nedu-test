/**
 * @deprecated Re-export stub — will be removed in Phase 8 cleanup.
 * Import from '@/lib/scoring' still works,
 * but prefer '@/features/maxdiff/scoring' for new code.
 */
export { calculateMaxDiffScores } from '@/features/maxdiff/scoring';
